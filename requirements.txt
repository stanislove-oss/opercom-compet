# Ноутбук использует вложенные кавычки в f-строках (PEP 701) — нужен Python >= 3.12.
pandas>=2.2
numpy>=1.26
python-pptx>=0.6.23
lxml>=5.0
openpyxl>=3.1
python-dotenv>=1.0

# Доверять сертификатам из хранилища системы: в корпоративной сети трафик идёт
# через прокси со своим корневым сертификатом, и чтение Google-таблицы
# со словарём чистки иначе падает на проверке сертификата.
truststore>=0.10

# База
clickhouse-connect>=0.8      # ClickHouse по HTTP(S) — боевой режим
pyodbc>=5.1                  # SQL Server — на время переезда, для сверки и отката
