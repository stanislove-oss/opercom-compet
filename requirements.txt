# Ноутбук использует вложенные кавычки в f-строках (PEP 701) — нужен Python >= 3.12.
pandas>=2.2
numpy>=1.26
python-pptx>=0.6.23
lxml>=5.0
openpyxl>=3.1
python-dotenv>=1.0

# Драйвер MySQL тянет внутренний пакет `app` (app.sql_oop.MySQL). Его в этом
# репозитории нет — установи его отдельно вместе с его зависимостями.
